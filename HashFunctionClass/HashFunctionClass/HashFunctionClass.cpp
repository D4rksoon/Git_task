#include <iostream>
#include <math.h>
#include "HashTable.h";

int main()
{
    std::vector<int> keys = { 10, 20, 30, 40 };
    int size = keys.size(); // длина вектора на входе
    int N = size + (size % 10); // Размер таблицы

}
